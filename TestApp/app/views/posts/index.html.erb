<h1>Todos los Posts</h1>

<table>
  <tr>
    <th>Titulo</th>
    <th>Autor</th>
    <th>Post</th>
  </tr>

  <% @post.each do |post| %>
      <tr>
        <td><%= post.title %></td>
        <td><%= post.user %></td>
        <td><%= post.text %></td>
      </tr>
  <% end %>
</table>